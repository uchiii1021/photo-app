<%= form_with model: @photo, local: true do |f| %>
  <%= image_tag @photo.image.url %>
  <%= f.text_field :title %>
  <%= f.text_area :description %>
  <%= f.submit "投稿する" %>
  <%= link_to "削除する", photo_path(@photo), method: :delete, data: { confirm: '削除しますか？' }%>
<% end %>
