<h1>DM</h1>
<h1>DM</h1>
<h1>DM</h1>

<% @user_rooms.each do |ur| %>
  <% if ur.user_id != current_user.id %>
    <%= link_to user_path(ur.user) do %>
      <%= ur.user.name %>さん
    <% end %>
  <% end %>
<% end %>

<% if @messages.present? %>
  <% @messages.each do |m| %>
    <%= m.user.name %>
    <%= m.message_content %>
  <% end %>
<% else %>
  <h3 class="text-center">メッセージはまだありません</h3>
<% end %>

<%= form_with model: @message, local: true do |f| %>
  <%= f.text_area :message_content, placeholder: "メッセージを入力して下さい", size: "40x2" %>
  <%= f.hidden_field :room_id, value: @room.id %>
  <%= f.submit '送信' %>
<% end %>
